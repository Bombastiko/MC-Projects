# API Version check
# Executed by other datapacks to verify if FuseBox is installed and what version is running

# Set version score (1 = v0.1 Alpha)
scoreboard players set #fb_installed fb.version 1
